streamlit>=1.35.0

# ── Vision / ML ──────────────────────────────────────────────────────────────
# opencv-python-headless MUST be listed before deep-sort-realtime.
# deep-sort-realtime declares `opencv-python` as a dependency; by pre-installing
# the headless variant uv/pip treats it as already satisfied and skips the full
# (libGL-dependent) build.
opencv-python-headless>=4.9.0
ultralytics>=8.2.0
torch>=2.2.0
torchvision>=0.17.0

# ── Tracking ─────────────────────────────────────────────────────────────────
# Install deep-sort-realtime WITHOUT its auto-pulled opencv-python by listing
# opencv-python-headless above (pip resolves cv2 as already present).
deep-sort-realtime>=1.3.2

# ── AI Report Providers ──────────────────────────────────────────────────────
google-generativeai>=0.7.0
openai>=1.30.0
anthropic>=0.28.0

# ── Data / Viz ───────────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
plotly>=5.22.0
Pillow>=10.0.0

# ── Export ───────────────────────────────────────────────────────────────────
fpdf2>=2.7.9
